var a : integer is -5;
var b : real is -10.75;

routine addNegativeNumbers(x : integer, y : real) : real is
    var sum : real;
    sum := x + y;
    return sum;
end

routine main() is
    var result : real;
    result := addNegativeNumbers(a, b);
    print result;
end
